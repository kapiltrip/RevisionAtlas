# Revision Atlas agent instructions

For any Markdown or documentation change in this repository, use `$maintain-github-markdown` and run its checker before committing. Keep the rendering rules in that skill instead of duplicating them here.
